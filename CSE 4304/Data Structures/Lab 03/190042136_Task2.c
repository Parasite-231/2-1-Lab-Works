#include <stdio.h>
#include <string.h>
#define MAX 101


int top = –1;
int stack[MAX];

void main()
{
    char expression[MAX];
    int i;

    printf("Enter your expression to check whether valid or not : ");
    gets(exp);
